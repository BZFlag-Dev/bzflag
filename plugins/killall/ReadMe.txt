========================================================================
    DYNAMIC LINK LIBRARY : killall Project Overview
========================================================================

This is the SAMPLE_PLUGIN. It makes a killall DESCRIPTION

Replace everyone in every file in here the term SAMPLE_PLUGIN with the name of your project
open then .sln and .vcproj files in a text editor and do a search and replace

