========================================================================
    DYNAMIC LINK LIBRARY : HTTPServer Project Overview
========================================================================

This is the HTTPServer. It makes the core HTTP system used by other plugins

This plugin is automaticly loaded by the first HTTP plugin to be loaded by
the server. It contains the shared networking components and Virtual Dir 
handling used by all HTTP plugins.
