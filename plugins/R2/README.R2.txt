BZFlag Server Plugin: R2
================================================================================

This plugin joins a single server-side player as player.  R2 is kind of a jerk
and will try to murder everyone.


Loading the plugin
--------------------------------------------------------------------------------

This plugin takes no optional arguments, so load it with:
  -loadplugin R2
