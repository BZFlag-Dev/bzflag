
libLuaBZFS source code

It was a plugin, but has now been merged into the main bzfs executable.
The bz_XXX() calls can be converted to native bzfs calls whenever it's
convenient or faster to do so.


