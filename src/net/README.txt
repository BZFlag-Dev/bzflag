This directory contains stuff for network handling.  This should be
generic code that could otherwise go into the common library, but is
specific to networking.  There should not be any code specific to the
bzflag network protocol in here.
